<div class="custom_card ">
    <h2>Lotos organisés <span class="badge badge-pill badge-primary"> {{user.LotoEvents|length}} </span> </h2>
    {% if user.canOrganize() %}
        <a class="btn btn-primary cursor angle-r" title="Ajouter un loto"><i class="fas fa-plus"></i></a>
        {% for loto in user.LotoEvents %}
            <li>
                <a>{{ loto.title }}</a>
            </li>
        {% else %}
            <li>Vous n'avez pas encore créé de loto</li>
        {% endfor %}

    {% else %}
    <p>Vous devez ajouter votre association pour pouvoir commencer à organiser des lotos live.</p>
    {% endif %}    
    <ul>
    
    </ul>
</div>