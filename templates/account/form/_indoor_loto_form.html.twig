 <div class="form">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary btn-block">{{ button_label }}</button>
        {{ form_end(form) }}
       
    </div>